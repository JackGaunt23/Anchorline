import { StubPage } from "@/components/stub-page";

export default function CallsPage() {
  return (
    <StubPage
      title="Calls"
      description="A searchable call log with recordings, transcripts, and per-call process scorecards. The Overview page's producer drill-down already shows recent scored calls."
      phase="Coming with the live RingCentral sync (Phase 2) and scoring pipeline (Phase 4)"
    />
  );
}
