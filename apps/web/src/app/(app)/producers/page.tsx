import { StubPage } from "@/components/stub-page";

export default function ProducersPage() {
  return (
    <StubPage
      title="Producers"
      description="Per-producer deep dives: score trends over time, call histories, and coaching notes. The Overview page's producer table and drill-down modal cover the essentials today."
      phase="Planned after the client-demo milestone"
    />
  );
}
