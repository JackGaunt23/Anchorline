import { StubPage } from "@/components/stub-page";

export default function ReportsPage() {
  return (
    <StubPage
      title="Reports"
      description="Exportable weekly and monthly performance reports built from the same synced data behind the Overview dashboard."
      phase="Planned after the client-demo milestone"
    />
  );
}
